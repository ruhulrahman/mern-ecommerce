const initState = {
    error: null,
    message: "",
    loading: false,
    success: false,
}

const orderReducer = (state = initState, action) => {


    return state
}

export default orderReducer